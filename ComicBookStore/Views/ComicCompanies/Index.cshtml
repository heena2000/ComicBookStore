@model IEnumerable<ComicBookStore.Models.ComicCompany>

@{
    ViewData["Title"] = "Index";
}

<h1 class="heading-font text-center">View All Comic Company Details</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Add New Comic Company</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-secondary" asp-route-id="@item.CompanyID">Edit</a> |
                <a asp-action="Details" class="btn btn-success" asp-route-id="@item.CompanyID">Details</a> |
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.CompanyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
